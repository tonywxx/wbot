# wbot 策略模块教学手册

> 本手册面向「想用 TA-Lib 编写并运行量化策略」的用户。
> 读完你将掌握：策略 DSL 的写法、如何用 `TA_*` 直接调用 TA-Lib 全部指标、
> 如何把策略写进 `strategy.toml` 并回测，以及两个完整可运行的实例。
>
> This manual explains the strategy module: the DSL, how to call any TA-Lib
> indicator via `TA_*`, how to register a rule in `strategy.toml`, and two
> complete, runnable examples.

---

## 1. 策略模块是什么 / What is the strategy module?

wbot 的策略系统由三层构成：

1. **指标层 (`src/indicators`)**：把一串 K 线（`Candle` = OHLCV）映射为一串 `f64`。
   既包含自研指标（`MA/SMA/EMA/MACD/RSI/KDJ/BOLL`），也通过 **TA-Lib 抽象 API**
   对接了 TA-Lib 的**全部**函数（约 161 个，见 `docs/ta-lib-indicators.bilingual.md`）。
2. **表达式层 (`src/signals/dsl`)**：用一套函数式 DSL 把指标/价格/数字组合成
   「条件表达式」（比较、交叉、逻辑与或非）。
3. **规则层 (`strategy.toml`)**：每条规则 = 一个方向（`buy`/`sell`）+ 一个 DSL 信号
   + 作用范围（`watchlist` 或具体代码）。信号引擎对每只标的逐根求值，沿触发
   （false→true 才发信号）去抖。

关键点：**只要 TA-Lib 支持的函数，你都能在 DSL 里直接写 `TA_<函数名>(...)` 来用**，
无需任何额外开发。本手册的实例全部基于 TA-Lib。

---

## 2. 策略 DSL 速查 / DSL cheat-sheet

### 2.1 语法（函数式，无中缀）

```
逻辑:  and(a, b, ...)   or(a, b, ...)   not(a)
比较:  gt(a,b)  lt(a,b)  gte(a,b)  lte(a,b)  eq(a,b)
交叉:  cross_above(a, b)        # a 从下向上穿过 b
       cross_below(a, b)        # a 从上向下穿过 b
操作数: 数字 | PRICE(src) | NAME(src, p1, p2, ...)[.field]
```

- **逻辑函数** `and/or/not` 的参数是「条件表达式」（即比较或交叉节点）。
- **比较/交叉** 的参数是「操作数」：数字、价格，或指标。
- 指标名大小写不敏感，DSL 会统一转大写。

### 2.2 操作数 / Operands

| 写法 | 含义 |
|------|------|
| `30` / `0.02` | 字面数字（用于阈值比较） |
| `PRICE(close)` | 当日收盘价序列（也可用 `open/high/low/volume`） |
| `TA_RSI(close, 14)` | TA-Lib 的 RSI，作用在收盘价，周期 14 |
| `TA_MACD(close,12,26,9).hist` | MACD 的柱状输出（多输出选择，见下） |
| `TA_BBANDS(close,20,2).lower` | 布林带下轨 |

### 2.3 TA-Lib 指标的调用约定 / Calling TA-Lib

- **函数名前缀 `TA_`**：`TA_RSI` → TA-Lib 的 `RSI`，`TA_BBANDS` → `BBANDS`，其余同理。
  函数名大小写不敏感（`ta_rsi`、`Ta_Rsi` 均可）。
- **第一个参数 = 价格来源**：TA-Lib 按函数自身的「价格掩码」取用所需分量
  （绝大多数用收盘价；需要高/低的函数会自动取用）。所以你一般写 `close` 即可。
- **其余参数 = 可选参数**：时间周期、MAType、偏差倍数等；**不写则取 TA-Lib 默认值**。
  例如 `TA_RSI(close)` 等价于 `TA_RSI(close, 14)`。
- **多输出选择（`.field`）**：部分函数有多个输出，用 `.0/.1/.2` 或输出名选取：
  - `TA_MACD(...)` → `.macd` / `.dea`(signal) / `.hist`，或 `.0/.1/.2`
  - `TA_BBANDS(...)` → `.upper` / `.middle` / `.lower`，或 `.0/.1/.2`
  - 不写 `.field` 时取**第一个**输出。
- **前导 `NaN`**：序列前若干根不足计算长度，输出为 `NaN`，不会触发信号（比较/交叉遇到
  `NaN` 视为「未触发」）。例如 `TA_RSI(close,14)` 前 13 根为 `NaN`。

> 完整参数表（默认值 / 范围 / 类型）见 `docs/ta-lib-indicators.bilingual.md`。
> 需要选择均线类型（MAType）时，用整数 0–8（0=SMA,1=EMA,2=WMA,3=DEMA,4=TEMA,
> 5=TRIMA,6=KAMA,7=MAMA,8=T3），如 `TA_MA(close, 20, 1)` 表示 20 周期 EMA。

---

## 3. 把策略写进 strategy.toml / Authoring a rule

每条规则是一个 `[[rules]]` 表：

```toml
[[rules]]
id      = "ta_momentum"          # 唯一标识
label   = "TA 动量共振买入"       # 展示名
side    = "buy"                  # "buy" 或 "sell"
scope   = "watchlist"            # "watchlist" 或 "600519,000858"
enabled = true                   # 是否启用
signal  = "and(cross_above(TA_RSI(close,14), 30), cross_above(TA_MACD(close,12,26,9).hist, 0))"
note    = "RSI 脱离超卖 且 MACD 柱状转正，动量共振。"   # 可选备注
```

- `signal` 是 DSL 文本。若用「形态规则」（`kind="pattern"`）则不用 `signal`，但本手册
  聚焦 TA-Lib + DSL，全部用 `signal`。
- 保存后运行回测即可（见第 4 节）。

---

## 4. 运行与回测 / Run & backtest

```bash
# A 股回测，报告输出到 ./reports
cargo run --example backtest_all

# 美股回测，输出到 ./reports_us
cargo run --example backtest_all us
```

回测引擎会对 `strategy.toml` 中**每条启用规则**在历史数据上求值，生成
`<id> 策略回测报告.md`（胜率按信号触发后持有 N 根的前向收益实测，非主观断言）。

> 本仓库已在 `strategy.toml` 末尾附带了本手册两个实例的规则，且默认 `enabled = true`（开箱即可回测）。
> 若不想让教学实例干扰你现有的策略，把对应规则的 `enabled` 改为 `false` 即可，无需改动其它任何代码。

---

## 5. 实例一：TA-Lib 动量共振策略 / Example 1 — Momentum Confluence

**思路**：单一指标容易假突破。我们用 TA-Lib 的两个动量指标做「共振」——
RSI 离开超卖区（动能修复）**且** MACD 柱状由负转正（趋势动能转多），两者同时成立才买入；
任一方转弱即离场。

### 5.1 完整规则（可直接粘贴到 strategy.toml）

```toml
[[rules]]
id = "ta_ex1_buy"
label = "实例一·TA 动量共振买入"
side = "buy"
scope = "watchlist"
enabled = true
signal = "and(cross_above(TA_RSI(close,14), 30), cross_above(TA_MACD(close,12,26,9).hist, 0))"
note = "实例一：RSI 上穿 30（脱离超卖）且 MACD 柱状上穿 0（动能转多），动量共振买入。"

[[rules]]
id = "ta_ex1_sell"
label = "实例一·TA 动量共振卖出"
side = "sell"
scope = "watchlist"
enabled = true
signal = "or(cross_below(TA_RSI(close,14), 70), cross_below(TA_MACD(close,12,26,9).hist, 0))"
note = "实例一：RSI 下穿 70（超买回落）或 MACD 柱状下穿 0（动能转空），离场。"
```

### 5.2 逐元素拆解 / Step-by-step

买入信号：

```
and(
  cross_above( TA_RSI(close,14), 30 ),        # 条件 A：RSI(14) 从下向上穿过 30
  cross_above( TA_MACD(close,12,26,9).hist, 0 )  # 条件 B：MACD 柱状 从下向上穿过 0
)
```

1. **`TA_RSI(close,14)`**：调用 TA-Lib 的 RSI，14 周期，输出值为 0–100 的振荡序列。
   前 13 根为 `NaN`，不参与判断。
2. **`cross_above(X, 30)`**：取 `X` 与阈值 `30` 两根「末值」，判断**上一根 ≤30 且
   本根 >30**——即「刚刚脱离超卖」。交叉对数字阈值同样适用（数字被展开为等长序列）。
3. **`TA_MACD(close,12,26,9).hist`**：TA-Lib 的 MACD，参数快 12 / 慢 26 / 信号 9；
   通过 `.hist` 选取**柱状**输出（= DIF − DEA）。`.hist` 也可用 `.2` 表示。
4. **`cross_above(..., 0)`**：柱状由负转正，代表下跌动能衰竭、反弹开始。
5. **`and(A, B)`**：A、B **同时**为真才买入——这就是「共振」，过滤掉单一指标的毛刺。

卖出信号把 `cross_above` 换成 `cross_below`，并把 RSI 阈值换成 70（超买区），
用 `or(...)` 表示「任一转弱即离场」。

### 5.3 如何验证 / Verify

- 想单独看指标数值，可参考 `examples/ta_indicators_list.rs` 的方式，用
  `IndicatorRegistry::eval(&id("TA_RSI", &[14.0], None), &series)` 打印序列，或在自己的临时脚本里调用同一 API。
- 回测：实例规则已默认启用，直接 `cargo run --example backtest_all`，查看
  `reports/ta_ex1_buy 策略回测报告.md` 的胜率与信号分布。

---

## 6. 实例二：TA-Lib 布林带均值回归 + ADX 过滤 / Example 2 — Bollinger Reversion

**思路**：布林带下轨是常见的「超跌反弹」参考位，但在强趋势中抄底会被套。
我们用 TA-Lib 的 **ADX** 判断市场是否处于「震荡区间」（ADX < 25），只在震荡里做
均值回归：价格从下轨反弹时买入，回到中轨时卖出；一旦 ADX 突然走强（趋势出现）也离场。

### 6.1 完整规则（可直接粘贴到 strategy.toml）

```toml
[[rules]]
id = "ta_ex2_buy"
label = "实例二·布林带下轨反弹买入(区间)"
side = "buy"
scope = "watchlist"
enabled = true
signal = "and(cross_above(PRICE(close), TA_BBANDS(close,20,2).lower), lt(TA_ADX(close,14), 25))"
note = "实例二：收盘价上穿布林下轨 且 ADX<25（震荡区间），均值回归买入。"

[[rules]]
id = "ta_ex2_sell"
label = "实例二·回归中轨或趋势转强卖出"
side = "sell"
scope = "watchlist"
enabled = true
signal = "or(cross_below(PRICE(close), TA_BBANDS(close,20,2).mid), gt(TA_ADX(close,14), 30))"
note = "实例二：收盘价跌破布林中轨 或 ADX>30（趋势转强），离场。"
```

### 6.2 逐元素拆解 / Step-by-step

买入信号：

```
and(
  cross_above( PRICE(close), TA_BBANDS(close,20,2).lower ),  # 条件 A：收盘价上穿下轨
  lt( TA_ADX(close,14), 25 )                                  # 条件 B：ADX < 25（震荡）
)
```

1. **`PRICE(close)`**：当日收盘价序列（等价于 `TA_*` 之外的「原生价格」操作数）。
2. **`TA_BBANDS(close,20,2).lower`**：TA-Lib 布林带，周期 20、偏离 2 倍标准差；
   通过 `.lower` 选取**下轨**输出（等价于 `.2`）。中轨/上轨分别为 `.mid`/`.upper`。
3. **`cross_above(PRICE(close), 下轨)`**：价格从下向上收复下轨——典型的超跌反弹起点。
4. **`TA_ADX(close,14)`**：TA-Lib 平均趋向指数，衡量**趋势强度**（不看方向），0–100。
   其值 < 25 一般视为「无趋势 / 震荡」，适合做均值回归。
5. **`lt(ADX, 25)`**：条件 B 用**比较**而非交叉——只要 ADX 低于 25 即满足。
6. **`and(A, B)`**：只在「价格反弹」**且**「市场震荡」时买入，规避单边下跌陷阱。

卖出信号：`cross_below(PRICE(close), 中轨)` 表示反弹到位、回到中枢即止盈；
`gt(TA_ADX(close,14), 30)` 表示趋势突然走强，区间策略失效，立即离场（`or` 任一成立即卖）。

### 6.3 参数可调点 / Tuning

- 把 `TA_BBANDS(close,20,2)` 改成 `TA_BBANDS(close,20,2.5)` 可放宽通道、减少假信号。
- ADX 阈值 25 / 30 可按标的波动率调整。
- 想用 EMA 而非 SMA 计算布林中轨？布林带中轨恒为 SMA，不可改；但你可以换用
  `TA_MA(close, 20, 1)`（= EMA）做自定义通道。

---

## 7. 进阶技巧 / Advanced

- **形态识别**：TA-Lib 的 61 个 `CDL*` 蜡烛图形态可直接用，输出为整数 0 / 100 / -100
  （100=看涨, -100=看跌）。例如看启明星买入：
  `eq(TA_CDLMORNINGSTAR(close), 100)`。详见双语文档的 Pattern Recognition 分组。
- **自定义 MAType**：对 `MA / MACD / BBANDS / MACDEXT / TEMA / DEMA` 等，最后一个整数参数
  多为 MAType（0–8）。
- **数学/统计辅助**：`TA_STDDEV`、`TA_LINEARREG_SLOPE`、`TA_CORREL` 等可用于构造
  自定义复合信号（如「价格偏离其线性回归线超过 N 倍标准差」）。
- **多周期**：带 `timeframe` 的规则会改用对应分钟 K 线求值（与现有形态规则一致），
  适合把实例做成日内版本。

## 8. 注意事项 / Caveats

- **价格掩码**：TA-Lib 函数自行决定用哪些 OHLCV 分量（多数为收盘价）。你在 DSL 里写的
  第一个 `close/open/high/low` 主要决定「主价格分量」的传递；若想对高价计算 EMA，
  当前通用封装会把该分量填入收盘价槽位——绝大多数场景用 `close` 即可。
- **前导 NaN**：任何 TA 指标在计算长度不足时返回 `NaN`，信号不会误触发，但回测起始段
  无信号属正常现象。
- **`enabled` 开关**：实例规则默认启用（已附在 `strategy.toml` 末尾），开箱即可回测；若不想让教学实例干扰现有策略，把对应规则的 `enabled` 改为 `false` 关闭即可。
- **依赖**：TA-Lib 由 `build.rs` 经 `pkg-config` 链接本机已安装的 TA-Lib（Homebrew：
  `brew install ta-lib`）。未安装时编译会报找不到 `ta-lib`。

---

## 9. 故障排查 / Troubleshooting

| 现象 | 原因与处理 |
|------|-----------|
| 编译报 `cannot find -lta-lib` | 本机未安装 TA-Lib。macOS：`brew install ta-lib`；Linux：`apt install ta-lib-dev`。 |
| 某 `TA_XXX` 完全没有信号 | 函数名拼错（区分大小写不敏感，但必须存在）；或数据长度不足导致全 `NaN`。 |
| `.field` 选错输出（如取到空序列） | 用 `docs/ta-lib-indicators.bilingual.md` 查该函数的输出名；也可改用 `.0/.1/.2`。 |
| 回测报告里胜率异常低 | 策略参数/阈值需针对标的优化；先用 `scope` 缩小到少数标的观察信号分布。 |
